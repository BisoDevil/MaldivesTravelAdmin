<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_send_notification"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.globalapp.maldivestravel.SendNotificationActivity">


    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textMultiLine"
        android:ems="10"
        android:id="@+id/txtNotification"
        android:layout_marginEnd="8dp"
        app:layout_constraintRight_toRightOf="@+id/activity_send_notification"
        android:layout_marginRight="8dp"
        android:layout_marginStart="8dp"
        app:layout_constraintLeft_toLeftOf="@+id/activity_send_notification"
        android:layout_marginLeft="8dp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toTopOf="@+id/activity_send_notification"
        android:hint="Write a Notification" />

    <Button
        android:text="Send"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/btnSendNotification"
        app:layout_constraintBottom_toBottomOf="@+id/activity_send_notification"
        app:layout_constraintRight_toRightOf="@+id/activity_send_notification"
        android:layout_marginBottom="16dp"
        app:layout_constraintLeft_toLeftOf="@+id/activity_send_notification"
        android:background="@drawable/loginbutton"
        android:typeface="normal"
        android:textSize="18sp"
        android:textStyle="normal|bold"
        android:textColor="@android:color/background_light"
        android:onClick="btnSend" />
</android.support.constraint.ConstraintLayout>
